'use strcit';

module.exports = function () {
    // drone endpoint pair
    this.endpoint = {
        'PUT': {
            'url': '',
            'emiiter': '',
        },
    };
    this.routeExecute = function (method, url) {
    }
};